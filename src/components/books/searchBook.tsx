import React from "react";
import { Form } from "./bookStyle";
import TextField from "@mui/material/TextField";

type Props = {
  searchValue: string;
  handleSearch: Function;
};
const SearchBook = (props: Props) => {
  return <>{/* écrire le code ici */}</>;
};
export default SearchBook;
